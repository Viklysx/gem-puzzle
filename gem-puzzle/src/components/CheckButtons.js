import PlayingField from './PlayingField';

class CheckButtons {
    constructor() {}

    // stirringBack(massMix) {
    //     const playing = new PlayingField();
    //     massMix = massMix.reverse();
    //     for (let i = 0; i < massMix.length; i++) {
    //         setTimeout(() => {
    //             playing.move(massMix[i]);
    //         }, 100 * (i + 0.2));
    //     }
    // }
}

export default CheckButtons;